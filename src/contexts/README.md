# Contexts

This directory contains React contexts for global state management.

## Planned contexts:
- `BoardContext.tsx` - Board state and actions
- `ThemeContext.tsx` - Theme preferences
- `ErrorContext.tsx` - Error handling and notifications
