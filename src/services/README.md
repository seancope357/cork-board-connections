# Services

This directory contains service layers for external integrations and API calls.

## Planned services:
- `api.ts` - API client configuration
- `boardService.ts` - Board CRUD operations
- `storageService.ts` - LocalStorage operations
- `exportService.ts` - Export functionality (JSON, PNG, PDF)
- `importService.ts` - Import and validation
